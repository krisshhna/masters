import { TexualDirective } from './texual.directive';

describe('TexualDirective', () => {
  it('should create an instance', () => {
    const directive = new TexualDirective();
    expect(directive).toBeTruthy();
  });
});
